extends ../layout

block content
  .container
    if user
    .page-header
      h3(align='center')  Nueva Direccion CloverCoin
      .newaddy #{address}
      .qrcode(align='center')
        a(href='CloverCoin:' + address)
          img(src='' + data_url)
      br
      p(align='center')
        a.btn.btn-default(href='/wallet', role='button') Regresa a tu Wallet.
